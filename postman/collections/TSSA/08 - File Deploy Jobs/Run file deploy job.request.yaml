$kind: http-request
url: "{{v1}}/file-deploy-jobs/{{id}}"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "targets": {
        "servers": [
          1
        ]
      }
    }
order: 1000
