$kind: http-request
url: "{{v1}}/deploy-jobs"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "name": "Deploy BLPackage",
      "package_id": 0,
      "targets": {
        "servers": [
          1
        ]
      }
    }
order: 2000
