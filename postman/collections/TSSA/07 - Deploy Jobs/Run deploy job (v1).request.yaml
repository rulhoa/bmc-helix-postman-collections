$kind: http-request
url: "{{v1}}/deploy-jobs/{{id}}"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "targets": {
        "servers": [
          1
        ],
        "server_groups": [
          3
        ]
      }
    }
order: 4000
